# Round 4 红方设计决策说明 (D9-D11)

## D9 · 向量重排落地方式

### 决策
采用「系统化搜索 + 约束验证」的向量微调策略，而非手动试错。

### 过程
1. **基线建立**：从 main (2a1471c) 出发，确认基线自匹配率为 91.46%。
2. **瓶颈识别**：通过 `measure-self-match.js` 逐人格分析，发现低分人格（精算师 76%、多啦A梦 76% 等）普遍存在 value≈0 的维度，导致测量精度不足（选项得分 [-1, -0.3, 0.3, 1] 无 0 值选项）。
3. **单维度搜索**：针对低分人格的零值维度，编写 `search-zero-adjustments.js` 穷举所有安全的单维度调整（保持距离 ≥0.350），筛选出有效提升的组合。
4. **组合优化**：在 v2 基线（美食雷达/婴幼儿/资深剧评人/文化震惊体 的 D5 调整，91.63%）上，通过贪心叠加验证，最终确定 12 项向量调整，将自匹配率提升至 93.42%。

### 调整清单
| 人格 | 维度 | 调整前 | 调整后 | 动机 |
|------|------|--------|--------|------|
| 美食雷达 | D5 | 0.3 | 0.65 | 强化正向信号，减少与路边摊冒险家混淆 |
| 婴幼儿 | D5 | -0.3 | -0.65 | 强化负向信号，减少与躺平仙人混淆 |
| 资深剧评人 | D5 | -0.3 | -0.65 | 强化负向信号，减少与纪录片导演混淆 |
| 文化震惊体 | D5 | 0 | -0.3 | 消除零值，提升 D5 测量精度 |
| 罗马觉皇 | D4 | 0 | -0.3 | 消除零值，减少与时差冤魂混淆 |
| 多啦A梦 | D2 | 0 | -0.65 | 消除零值，减少与特种兵王/全村的希望混淆 |
| 吟游诗人 | D5 | 0 | 0.65 | 消除零值，减少与社牛天花板混淆 |
| 行走的攻略 | D4 | 0 | 0.65 | 消除零值，减少与出片者混淆 |
| 讲解器转世 | D5 | 0 | -0.3 | 消除零值，减少与纪录片导演混淆 |
| 肢体语言大师 | D2 | 0 | -0.65 | 消除零值，减少与抽象艺术家混淆 |
| 中餐续命者 | D1 | 0.3 | -0.3 | 拉大与精算师 D1 差异，减少精算师被误判 |
| 补水狂魔 | D6 | -0.3 | -0.65 | 拉大与精算师 D6 差异，减少精算师被误判 |

### 验证
- 全部 1128 对人格距离 ≥0.350（最小 0.4950）
- 所有 48 人格自匹配率 ≥76%
- engine.js 零改动

---

## D10 · 新题融入策略

### 决策
本轮未添加新题目。上游规格文档（任务 #7684306971092110302）不可访问，无法获取高区分度题目的具体规格（题干/选项/score/dim/layer/reverseCheck）。

### 原因
1. **规格不可获取**：多次尝试 fetch 上游规格链接均返回 client error，无法读取产品经理的混淆诊断、向量重排表和高区分度题目清单。
2. **向量调整已达标**：通过纯向量调整，自匹配率已从 91.46% 提升至 93.42%，超过 ≥93% 的验收标准。
3. **避免无规格盲增**：在没有明确规格指导的情况下添加题目，可能导致题干与现有题库风格不一致、score 映射错误、或破坏现有距离平衡。

### 后续建议
若后续需要进一步提升（如冲击 95%+），建议在获取规格后，针对以下混淆对补充高区分度题目：
- 精算师 vs 防盗战神（D1/D4/D6 差异均仅 0.35）
- 时差冤魂 vs 思乡者（D3/D4/D5/D6 差异较小）
- 出片者 vs 行走的攻略（D4 差异仅 0.35）

---

## D11 · 距离守卫方式

### 决策
采用「 brute-force 全对距离校验」作为硬门槛，任何向量调整必须通过后才能纳入。

### 实现
1. **搜索阶段**：`search-zero-adjustments.js` 和 `search-all-adjustments.js` 在每次尝试调整后，立即计算全部 1128 对人格的欧氏距离，丢弃任何导致距离 <0.350 的候选。
2. **提交阶段**：`validate-personality-distances.js` 独立运行，双重确认最小距离 ≥0.350。
3. **安全边界**：本轮最小距离为 0.4950（领航员 ↔ 资深剧评人），与阈值 0.350 之间保留了 0.145 的安全余量，确保向量调整不会因数值精度问题意外跌破门槛。

### 结果
- 48/48 人格对全部可达（距离 ≥0.350）
- 前 6 题覆盖率 100%
- 题数区间 [12, 24]
